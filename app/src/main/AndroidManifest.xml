<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- For Android 13+ use the scoped media permissions for images/videos; keep legacy permissions for older devices -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Legacy permissions kept for devices < Android 13 (maxSdkVersion=32) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- Allow manager-level external storage access (requires user approval via settings on Android 11+). -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Android 14+ granular foreground-service permissions required when using foregroundServiceType="camera" or "location" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Declare camera-related features so Lint recognizes the CAMERA permission has matching hardware.
         Mark as not required so devices without a camera can still install the app. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Optional: declare autofocus feature if your app can take advantage of it. Keep not required. -->
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name="cam.et.dashcamsystem.app.DashcamApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DashcamSystem">
        <activity
            android:name=".app.activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DashcamSystem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Boot launch helper activity used as a full-screen intent target -->
        <activity
            android:name=".BootLaunchActivity"
            android:exported="true"
            android:theme="@style/Theme.DashcamSystem" />

        <service
            android:name="cam.et.dashcamsystem.app.services.BackgroundCameraService"
            android:exported="false"
            android:foregroundServiceType="camera|location" />

        <!-- Immortal foreground service -->
        <service
            android:name="cam.et.dashcamsystem.app.services.ImmortalService"
            android:exported="false"
            android:foregroundServiceType="camera|location" />

        <!-- Receiver used to restart the service after being scheduled by AlarmManager -->
        <receiver
            android:name="cam.et.dashcamsystem.app.receivers.RestartReceiver"
            android:exported="false" />

        <!-- Receiver to handle scheduled activity launches -->
        <receiver
            android:name="cam.et.dashcamsystem.app.receivers.ActivityLaunchReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="cam.et.dashcamsystem.ACTION_LAUNCH_MAIN" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver to restart service after device reboot -->
        <receiver
            android:name="cam.et.dashcamsystem.app.receivers.BootReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>